# ── Core API ──────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.1
pydantic>=2.10.0
pydantic-settings>=2.6.0

# ── Data processing ───────────────────────────────────────
pandas>=2.2.0
openpyxl>=3.1.2
numpy>=1.26.0

# ── HTTP client (for calling external APIs) ───────────────
httpx>=0.27.0

# ── Vercel serverless adapter ─────────────────────────────
mangum>=0.17.0

# ── Testing ───────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0

# ── Add these later when you are ready ───────────────────
# sqlalchemy>=2.0.0         (when you add a database)
# alembic>=1.13.0           (database migrations)
# psycopg2-binary>=2.9.9   (needs PostgreSQL: brew install postgresql)
# celery>=5.4.0             (background task queue)
# redis>=5.0.0              (needs Redis: brew install redis)
# scikit-learn>=1.5.0       (when you build the ML model)
